extends /layout/_application
block body
  div.wrapper-common.wrapper-pitch(data-path="pitch")
    div.main
      button(onclick='this.innerText = togglePlayback()') use demo audio
      button(onclick='toggleLiveInput()') use live input
      button(onclick='toggleOscillator()') use oscillator
      // |<button onclick="updatePitch(0);">sample</button>
      #detector.vague
        .pitch
          span#pitch --
          | Hz
        .note
          span#note --
        |    
        canvas#output(width='300', height='42')
        #detune
          span#detune_amt --
          span#flat cents &flat;
          span#sharp cents &sharp;
    canvas#waveform(width=512 height=256)
    canvas#history(width=512 height=256)
  script(src="js/pitchdetect.js")
